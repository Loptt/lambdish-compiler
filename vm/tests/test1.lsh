func sum :: num x, num y, (bool => num) f1 => num (
    
)

func sum2 :: num x, (num => num) f => num (
    *(
        +(x, 3),
        f(x)
    )
)

sum2(
    1,
    (# num x => num (
        *(x, 4)
    )
))