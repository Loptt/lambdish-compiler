func getEven :: [num] l => [num] (
    if (empty(l),
        [num],
        if (equal(%(head(l), 2), 0),
            insert(head(l), getEven(tail(l))),
            getEven(tail(l))
        )
    )
)

getEven([1,2,3,4])