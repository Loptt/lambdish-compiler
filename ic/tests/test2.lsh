func sum :: num x, num y => num (
    +(x, y)
)

func sum2 :: bool x, num y, num z => num (
    + (
        /(
            -(sum(3, y)),
            +(sum2(
                equal(1, y), 3, 1
            ))
        ), 
        *(x, 12)
    )
)

sum(sum2(<(2,3),4,7),2)