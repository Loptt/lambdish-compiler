sum :: num l => num (
	if(empty(l), 
        0,
        +(head(l), sum(tail(l)))
    )
)

reverse :: [char] l => [char] (
	if(empty(l),
		[],
		append( reverse(tail(l)) , [head(l)])
	)
)

sum([3, 2, 4, 6, 7])
reverse(['h','o','l','a'])
