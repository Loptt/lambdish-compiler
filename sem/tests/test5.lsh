func filterOdd :: [num] l => [num] (
    (# num x => ( %(x,2) ) ) ( head(l) )
)

func map :: [num] l, (num, [char] => char) f => [num] (
    append(f(head(l)), map(tail(l), f)
)

map([1,2,3,4], filterOdd)

filterOdd( [1,2,3,4,5,6,7] )